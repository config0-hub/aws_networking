# VPC peering acceptance Terraform

Accepts a pending VPC peering connection in account A and adds VPC-A's route to VPC-B. It also publishes the accepted connection and both VPC routing values for the final account-B route stack.
